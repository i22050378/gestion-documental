# Puerto donde corre este servicio (la app .NET corre en 5080, esta en 4000)
PORT=4000

# Conexion a MongoDB (el contenedor gd_mongo). Si cambiaste MONGO_PORT, ajusta el puerto.
MONGO_URI=mongodb://localhost:27017
MONGO_USER=mongoadmin
MONGO_PASS=M0ngo!2026
MONGO_AUTH_SOURCE=admin
MONGO_DB=indexacion_db
